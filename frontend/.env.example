# Optional: set this if your backend is not behind Vite's /api proxy.
# Use origin only (no trailing /api). Example: http://localhost:3000
VITE_API_BASE_URL=

# Dev-only proxy target used by vite.config.ts for /api requests.
VITE_API_PROXY_TARGET=http://localhost:3000
